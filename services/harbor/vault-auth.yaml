apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: harbor-vault-auth
  namespace: harbor
spec:
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: harbor
    serviceAccount: harbor-vault-auth
    audiences:
      - vault